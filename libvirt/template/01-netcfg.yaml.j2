network:
  version: 2
  ethernets:
    enp1s0:
      dhcp4: false
      addresses:
        - {{ ip }}/24
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
        addresses:
          - {{ dnsserver }}